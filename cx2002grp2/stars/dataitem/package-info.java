/**
 * A collection of data item (entity classes) used by the STARS system.
 * <p>
 * The relationship between data items will be synchronized automatically. For
 * example, if the course code of a course index is changed, the old course code
 * object will remove the course index from its index list, and the new course
 * code object will add the course index into its index list.
 */
package cx2002grp2.stars.dataitem;
